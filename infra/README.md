# Infra

This folder will contain:

- `supabase-schema.sql` – the canonical database schema for ProxyOS.
- Optional migration files if we later adopt a formal migration tool.

Apply the schema to your Supabase project via the SQL editor before running
the backend or frontend against a real database.

