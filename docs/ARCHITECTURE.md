# ProxyOS Architecture (Draft)

This document is a high-level companion to the Kimi / ProxyOS product spec.
It will be filled in as the implementation lands.

- Frontend: `apps/web` (Next.js, Tailwind, Phaser, Three.js, Supabase client)
- Backend: `apps/backend` (Express on a Hugging Face Docker Space)
- Database: Supabase Postgres + Realtime

See the root `README.md` for an overview and the code for the source of truth.

